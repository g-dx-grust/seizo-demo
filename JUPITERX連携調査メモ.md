# JupiterX 連携調査メモ（一志精工電機様 / G-DX）

> 商談の想定問答用カンペ。**「JupiterXの稼働データをどうやって取るのか」への回答**をこの1枚に集約。
> 調査日: 2026-08-17 ／ 調査範囲: 公開情報・実機スクショ・未ログインで確認できる範囲まで（お客様アカウントでのログインは未実施）

---

## 30秒で言える結論

- **出口の存在・ログイン方式・画面の作りまで、実機と実サーバーで確認済み。**
- **RPA不要**。ログイン→セッション保持→ページ／CSV取得、という軽いスクリプトを**毎晩バッチで自動実行できる構造**。
- 稼働状況（現在の状態）も、履歴CSVと**同じ認証ルート**で取得可能。アラーム検知→Lark通知までこの仕組みで繋がる。
- 残るのは「お客様アカウントを借りて、CSVの正確なURL・粒度を1回確認する」だけ。**技術的なブロッカーは無し。**

---

## 1. 確認できた事実（推測でなく事実）

| 項目 | 内容 | 確認方法 |
|---|---|---|
| 実ホスト | `www.jupiter-x.io`（スクショの `jupiter-x.io` はChromeのwww省略表示） | DNS / whois。GMO系レジストラ・2021年から稼働 |
| アクセス制御 | 未ログインで `/activity/` → `/auth/`（ログイン画面）へ **302リダイレクト**。全画面がセッション保護 | 実際にHTTPで確認 |
| 技術スタック | nginx + PHP 7.3 + **Laravel**（`laravel_session` Cookie＋CSRFトークン、セッション寿命30時間） | レスポンスヘッダ・Cookie |
| ログイン方式 | `POST /auth/` に `id`・`pw`・`_token`(CSRF) を送るだけの**古典的フォーム認証**。SSO・二段階認証・CAPTCHA無し | ログインフォームのHTML |
| 画面の作り | jQuery 1.12.4 + excanvas + html5shiv の**サーバーサイドHTML描画**（SPAではない）→ HTMLパースが素直に効く | ログイン画面のJS参照 |
| CSV出口 | 「機械の稼動履歴」画面に **「データをダウンロード(CSV)」ボタンが実在**。kW値が分オーダーで記録、年は2020まで遡れる（履歴蓄積済み） | ヒアリング実機スクショ photo-02 |
| 稼働状態の種類 | 稼動／準備／待機／停止中・オフライン／アラーム。機械グループはマシニング・ワイヤー放電・プロファイル研磨・平面研磨機、11〜12台 | 実機スクショ photo-01。モックの24hタイムラインと一致 |

---

## 2. データの取り方（2系統）

### A. 履歴・実績データ（日次バッチ向き）
「機械の稼動履歴」の **CSVダウンロードURL** を、ログイン済みセッションで機械×期間ぶん叩いて保存。
- セッションが30時間持つ → 日次23:00バッチと相性が良い
- 手動でも今日から運用可能（画面のボタンを押すだけ）。自動化はその上乗せ

### B. 稼働状況＝現在の状態（準リアルタイム向き）
「状態別稼動状況一覧」(`/activity/`) を数分おきに取得。取得ルートは当日ログインして5分で優先順位が決まる:

1. **本命: `/activity/` のHTMLを定期取得してパース** — カードの色＝現在状態、表＝状態別累計時間。サーバー描画HTMLなので確実
2. **理想: 裏にJSON/XHRがあればそれを使う** — 開発者ツールでF5一発で判定。あれば一番きれい
3. **代替: 履歴CSVの最新行から導出** — 最新スロット≒現在状態

→ どのルートでも**アラーム状態を拾える**ので、「アラーム検知 → Lark自動通知」のデモは実現可能。

---

## 3. 正直に言っておく制約（聞かれたら）

- **「リアルタイム」の鮮度** = ポーリング間隔 ＋ JupiterX自身の更新遅延。実機は当日13:38時点で当日データが出ており鮮度は良さそうだが、センサー→反映の遅延はデータ調査で実測。モックの「秒で点滅するライブバッジ」は演出で、**実物は5分粒度の準リアルタイム**。
- **CSVの正確なURL・パラメータ・粒度（5分刻みか）** はログイン後にしか見えない → データ調査でアカウントを借りて最終確認。
- **認証情報の扱い** — 実運用ではお客様側の環境変数等にIDPWを置き、スクリプトが読む建付け（平文で持たない）。
- **ベンダー(CIMX)への一報を推奨** — 自動アクセスの規約確認＋PHP 7.3（サポート終了版）の古めのシステムなので、負荷をかけない取得頻度をすり合わせ。

---

## 4. 想定問答（Q&A）

**Q. JupiterXのデータ、どうやって取るの？API あるの？**
> APIは公開されていませんが不要です。ログイン方式まで実機で確認済みで、CSV出力の出口とセッション認証を使えば、RPAなしのスクリプトで毎晩自動取得できます。

**Q. 稼働状況（今どの機械が止まってるか）もリアルタイムで取れる？**
> 取れます。稼働状況一覧のページを数分おきに読むだけで、現在状態とアラームが拾えます。厳密には5分粒度の準リアルタイムです。

**Q. JupiterXを改修する必要は？**
> 不要です。今ある画面とCSV出口をそのまま使います。御社システムには一切手を入れません。

**Q. すぐ始められる？**
> CSVは手動でも今日から運用できます。自動化と現在状態の取得方式を、データ調査フェーズでアカウントを借りて確定します。

---

## 5. データ調査フェーズの確認項目（当日やること）

- [ ] お客様アカウントでログインし、CSVダウンロードの実URL・パラメータを記録
- [ ] CSVを1本落として粒度（5分刻みか）・列構成（状態列の有無）を確認
- [ ] `/activity/` の裏にJSON/XHRがあるか開発者ツールで確認（ルート2の可否）
- [ ] センサー→JupiterX反映の遅延を実測
- [ ] CIMXへ自動取得の可否・推奨頻度を確認

※本メモは公開情報と未ログインで確認できる範囲の調査に基づく。お客様テナントへのログインは未実施。
